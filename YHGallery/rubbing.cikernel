kernel vec4 coreImageKernel(__sample image,float offset,float fa,__color color)
{
    vec2 cor = samplerCoord(image);
    vec4 last = sample(image,cor);
    if (cha < fa){
        return color;
    }else{
        return vec4(1,1,1,1);
    }
}
